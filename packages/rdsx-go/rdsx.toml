[extension]
name = "rdsx-go"
version = "0.0.1"
kind = "analyzer"
description = "Go analyzer adapter for Radius (gopls)"
entry = "src/main.ts"

[analyzer]
language_ids = ["go"]
depth_max = 4

[runtime]
engine = "bun"
min_version = "1.0.0"
