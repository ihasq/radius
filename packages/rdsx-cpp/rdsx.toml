[extension]
name = "rdsx-cpp"
version = "0.0.1"
kind = "analyzer"
description = "C/C++ analyzer adapter for Radius (clangd)"
entry = "src/main.ts"

[analyzer]
language_ids = ["c", "cpp"]
depth_max = 4

[runtime]
engine = "bun"
min_version = "1.0.0"
