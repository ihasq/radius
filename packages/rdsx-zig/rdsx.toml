[extension]
name = "rdsx-zig"
version = "0.0.1"
kind = "analyzer"
description = "Zig analyzer adapter for Radius (zls)"
entry = "src/main.ts"

[analyzer]
language_ids = ["zig"]
depth_max = 4

[runtime]
engine = "bun"
min_version = "1.0.0"
